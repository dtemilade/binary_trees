#include <binary_trees.h>

/*
 * binary_trees_ancestor - prototype function for lowest common ancestor
 * @first: variable pointer to the first node
 * @second: variable pointer to the second node
 */

binary_tree_t *binary_trees_ancestor(const binary_tree_t *first, const
		binary_tree_t *second);
{
}
